interface:
  display_name: "Project Context Audit"
  short_description: "Recover current SaigonPlanTravel source truth before coding"
  default_prompt: "Use $project-context-audit to inspect the current repository state and tell me the smallest correct next step without modifying production code."
policy:
  allow_implicit_invocation: true
