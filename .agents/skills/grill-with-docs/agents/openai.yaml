interface:
  display_name: "Grill With Docs"
  short_description: "Clarify features and update durable docs before coding"
  default_prompt: "Use $grill-with-docs to inspect the repository, interview me one question at a time, and lock the requirements before implementation."
policy:
  allow_implicit_invocation: true
